export type RpcNode = {
  name: string;
  url: string;
  latency: number;
};

export type UserInfo = {
  user_name: string;
  avater: string;
  user_email: string;
  address: string;
  user_id: string;
}

export type GUILD = "PSYDUCK";

